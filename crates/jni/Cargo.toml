[package]
name = "mterm-jni"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "JNI bridge antara Rust core dan Android chrome (Kotlin/Compose)."

[lib]
crate-type = ["cdylib"]

[dependencies]
mterm-core = { path = "../core" }
mterm-pty  = { path = "../pty" }
vte.workspace = true
once_cell = "1"
jni = { version = "0.21", default-features = false }